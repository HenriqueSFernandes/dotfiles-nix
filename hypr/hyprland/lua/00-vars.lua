-- Shared variables for the Hyprland Lua config.
-- Required by binds.lua and any other module that needs these values.

local M = {}

M.mod = "SUPER"
M.terminal = "ghostty"
M.menu = "rofi -show drun"
M.fileManager = "thunar"

return M
